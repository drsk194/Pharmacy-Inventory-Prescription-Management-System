# PIPMS Selenium Full-App Test Bundle

See `selenium_tests/README.md` for installation, credential variables, and execution commands.

The suite is intentionally external to the application source so you can use the same tests against dev/staging builds.
