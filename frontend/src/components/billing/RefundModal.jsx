import { useState } from "react";
import Modal from "../common/Modal";
import { billingApi } from "../../api/billingApi";
export default function RefundModal({ billId, onClose, onSaved }) { const [form, setForm] = useState({ amount: "", reason: "", linkedReturnId: "" }); const [error, setError] = useState(""); async function submit(event) { event.preventDefault(); try { await billingApi.createRefund({ billId, ...form, linkedReturnId: form.linkedReturnId || undefined }); onSaved(); } catch (err) { setError(err.response?.data?.message || "Could not record refund."); } } return <Modal title="Record Refund" onClose={onClose}><form className="modal-form" onSubmit={submit}><label>Amount<input type="number" min="0.01" step="0.01" value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} required /></label><label>Reason<textarea value={form.reason} onChange={(event) => setForm({ ...form, reason: event.target.value })} required /></label><label>Linked return ID<input value={form.linkedReturnId} onChange={(event) => setForm({ ...form, linkedReturnId: event.target.value })} /></label>{error && <p className="form-error">{error}</p>}<button type="submit">Record refund</button></form></Modal>; }
