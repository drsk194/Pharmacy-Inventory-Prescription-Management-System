import { useState } from "react";
import Modal from "../common/Modal";
import { dispensingApi } from "../../api/dispensingApi";
export default function ErrorReportModal({ dispensingId, onClose, onSaved }) { const [form, setForm] = useState({ errorType: "OTHER", description: "", correctiveAction: "" }); async function submit(event) { event.preventDefault(); await dispensingApi.submitError({ dispensingId, ...form }); onSaved(); } return <Modal title="Report Dispensing Error" onClose={onClose}><form className="modal-form" onSubmit={submit}><label>Error type<select value={form.errorType} onChange={(event) => setForm({ ...form, errorType: event.target.value })}><option>WRONG_DRUG</option><option>WRONG_DOSAGE</option><option>WRONG_PATIENT</option><option>LABEL_ERROR</option><option>OTHER</option></select></label><label>Description<textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} required /></label><label>Corrective action<textarea value={form.correctiveAction} onChange={(event) => setForm({ ...form, correctiveAction: event.target.value })} required /></label><button type="submit">Submit report</button></form></Modal>; }
