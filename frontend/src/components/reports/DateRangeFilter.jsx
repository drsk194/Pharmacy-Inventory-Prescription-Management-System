export default function DateRangeFilter({ startDate, endDate, onChange }) { return <div className="filter-row"><label className="checkbox-label">From<input type="date" value={startDate} onChange={(event) => onChange({ startDate: event.target.value, endDate })} /></label><label className="checkbox-label">To<input type="date" value={endDate} onChange={(event) => onChange({ startDate, endDate: event.target.value })} /></label></div>; }
