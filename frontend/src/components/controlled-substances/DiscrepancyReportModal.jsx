import { useEffect, useState } from "react";
import Modal from "../common/Modal";
import { controlledSubstanceApi } from "../../api/controlledSubstanceApi";
import { drugApi } from "../../api/drugApi";
export default function DiscrepancyReportModal({ onClose, onSaved }) { const [drugs, setDrugs] = useState([]); const [form, setForm] = useState({ drugId: "", countedQuantity: "", notes: "" }); const [error, setError] = useState(""); useEffect(() => { drugApi.list({ page: 0, size: 500, activeOnly: true }).then((response) => { const data = response.data.data; setDrugs(data.content || data); }).catch(() => setError("Could not load drugs.")); }, []); async function submit(event) { event.preventDefault(); try { await controlledSubstanceApi.createDiscrepancy({ ...form, countedQuantity: Number(form.countedQuantity) }); onSaved(); } catch (err) { setError(err.response?.data?.message || "Could not submit report."); } } return <Modal title="Report Discrepancy" onClose={onClose}><form className="modal-form" onSubmit={submit}><label>Drug<select value={form.drugId} onChange={(event) => setForm({ ...form, drugId: event.target.value })} required><option value="">Select...</option>{drugs.map((drug) => <option key={drug.id} value={drug.id}>{drug.brandName ? `${drug.genericName} (${drug.brandName})` : drug.genericName}</option>)}</select></label><label>Counted quantity<input type="number" min="0" value={form.countedQuantity} onChange={(event) => setForm({ ...form, countedQuantity: event.target.value })} required /></label><label>Notes<textarea value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} required /></label>{error && <p className="form-error">{error}</p>}<button type="submit">Submit report</button></form></Modal>; }
