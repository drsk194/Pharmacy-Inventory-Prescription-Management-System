import { useState } from "react";
import Modal from "../common/Modal";
import { controlledSubstanceApi } from "../../api/controlledSubstanceApi";
export default function DiscrepancyReportModal({ onClose, onSaved }) { const [form, setForm] = useState({ batchId: "", description: "" }); const [error, setError] = useState(""); async function submit(event) { event.preventDefault(); try { await controlledSubstanceApi.createDiscrepancy(form); onSaved(); } catch (err) { setError(err.response?.data?.message || "Could not submit report."); } } return <Modal title="Report Discrepancy" onClose={onClose}><form className="modal-form" onSubmit={submit}><label>Batch ID<input value={form.batchId} onChange={(event) => setForm({ ...form, batchId: event.target.value })} required /></label><label>Description<textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} required /></label>{error && <p className="form-error">{error}</p>}<button type="submit">Submit report</button></form></Modal>; }
