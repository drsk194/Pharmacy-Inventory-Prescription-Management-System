import { useState } from "react";
import Modal from "../common/Modal";
import { controlledSubstanceApi } from "../../api/controlledSubstanceApi";
export default function PinSetupModal({ onClose, onSaved }) { const [pin, setPin] = useState(""); const [confirm, setConfirm] = useState(""); const [error, setError] = useState(""); async function submit(event) { event.preventDefault(); if (pin !== confirm) { setError("PINs do not match."); return; } try { await controlledSubstanceApi.setPin({ pin }); onSaved(); } catch (err) { setError(err.response?.data?.message || "Could not set PIN."); } } return <Modal title="Set Controlled Substance PIN" onClose={onClose}><form className="modal-form" onSubmit={submit}><label>New PIN<input type="password" inputMode="numeric" minLength="4" value={pin} onChange={(event) => setPin(event.target.value)} required /></label><label>Confirm PIN<input type="password" inputMode="numeric" minLength="4" value={confirm} onChange={(event) => setConfirm(event.target.value)} required /></label>{error && <p className="form-error">{error}</p>}<button type="submit">Set PIN</button></form></Modal>; }
