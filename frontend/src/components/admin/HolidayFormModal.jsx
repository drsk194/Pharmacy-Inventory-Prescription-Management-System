import { useState } from "react";
import Modal from "../common/Modal";
import { scheduleApi } from "../../api/scheduleApi";
export default function HolidayFormModal({ onClose, onSaved }) { const [form, setForm] = useState({ description: "", date: "", closed: true }); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); async function submit(event) { event.preventDefault(); setSaving(true); setError(""); try { await scheduleApi.createHoliday(form); onSaved(); } catch (err) { setError(err.response?.data?.message || "Could not add holiday."); } finally { setSaving(false); } } return <Modal title="Add Holiday" onClose={onClose}><form className="modal-form" onSubmit={submit}><label>Description<input value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} placeholder="e.g. Pongal" required /></label><label>Date<input type="date" value={form.date} onChange={(event) => setForm({ ...form, date: event.target.value })} required /></label><label className="checkbox-label"><input type="checkbox" checked={form.closed} onChange={(event) => setForm({ ...form, closed: event.target.checked })} />Pharmacy closed on this day</label>{error && <p className="form-error">{error}</p>}<button type="submit" disabled={saving}>{saving ? "Adding…" : "Add holiday"}</button></form></Modal>; }
