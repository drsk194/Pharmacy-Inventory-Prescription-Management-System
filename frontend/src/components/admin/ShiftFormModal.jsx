import { useState } from "react";
import Modal from "../common/Modal";
import { scheduleApi } from "../../api/scheduleApi";
export default function ShiftFormModal({ onClose, onSaved }) { const [form, setForm] = useState({ name: "", startTime: "", endTime: "" }); const [error, setError] = useState(""); async function submit(event) { event.preventDefault(); try { await scheduleApi.createShift(form); onSaved(); } catch (err) { setError(err.response?.data?.message || "Could not create shift."); } } return <Modal title="Create Shift" onClose={onClose}><form className="modal-form" onSubmit={submit}><label>Name<input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} required /></label><label>Start time<input type="time" value={form.startTime} onChange={(event) => setForm({ ...form, startTime: event.target.value })} required /></label><label>End time<input type="time" value={form.endTime} onChange={(event) => setForm({ ...form, endTime: event.target.value })} required /></label>{error && <p className="form-error">{error}</p>}<button type="submit">Create shift</button></form></Modal>; }
