import { useCallback, useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import { useAuth } from "../../hooks/useAuth";
import { patientApi } from "../../api/patientApi";
import Pagination from "../../components/common/Pagination";
import PatientFormModal from "../../components/patients/PatientFormModal";
export default function PatientListPage() { const { user } = useAuth(); const canManage = user?.roles?.includes("ROLE_ADMIN"); const [rows, setRows] = useState([]); const [page, setPage] = useState(0); const [totalPages, setTotalPages] = useState(0); const [search, setSearch] = useState(""); const [error, setError] = useState(""); const [open, setOpen] = useState(false); const navigate = useNavigate(); const load = useCallback(async () => { try { const response = await patientApi.list({ page, size: 20, search: search || undefined }); const data = response.data.data; setRows(data.content || data); setTotalPages(data.totalPages || 0); } catch (err) { setError(err.response?.data?.message || "Couldn't load patients."); } }, [page, search]); useEffect(() => { const timer = setTimeout(load, 300); return () => clearTimeout(timer); }, [load]); return <main className="list-page"><div className="list-page__header"><h1>Patients</h1>{canManage && <button type="button" onClick={() => setOpen(true)}>Create Patient</button>}</div><input className="list-page__search" type="search" placeholder="Search patients..." value={search} onChange={(event) => { setSearch(event.target.value); setPage(0); }} />{error && <p className="form-error">{error}</p>}<table className="data-table"><thead><tr><th>Name</th><th>Date of birth</th><th>Phone</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id} onClick={() => navigate(`/patients/${row.id}`)}><td>{row.fullName}</td><td>{row.dateOfBirth}</td><td>{row.phoneNumber}</td></tr>)}{!rows.length && <tr><td colSpan="3">No patients found.</td></tr>}</tbody></table><Pagination page={page} totalPages={totalPages} onPageChange={setPage} />{open && canManage && <PatientFormModal onClose={() => setOpen(false)} onSaved={() => { setOpen(false); load(); }} />}</main>; }
