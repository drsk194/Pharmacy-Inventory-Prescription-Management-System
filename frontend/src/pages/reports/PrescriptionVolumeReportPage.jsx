import ReportView from "../../components/reports/ReportView"; import { reportApi } from "../../api/reportApi"; const COLUMNS = [{ key: "date", label: "Date" }, { key: "prescriptionCount", label: "Prescriptions" }]; export default function PrescriptionVolumeReportPage() { return <ReportView title="Prescription Volume" columns={COLUMNS} fetcher={reportApi.getPrescriptionVolume} exportFetcher={reportApi.exportPrescriptionVolume} />; }
