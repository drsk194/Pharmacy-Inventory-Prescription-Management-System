import ReportView from "../../components/reports/ReportView"; import { reportApi } from "../../api/reportApi"; const COLUMNS = [{ key: "drugName", label: "Drug" }, { key: "totalStock", label: "Total stock" }, { key: "totalValue", label: "Total value" }]; export default function InventorySummaryReportPage() { return <ReportView title="Inventory Summary" columns={COLUMNS} fetcher={reportApi.getInventorySummary} />; }
