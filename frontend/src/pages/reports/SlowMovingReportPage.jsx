import ReportView from "../../components/reports/ReportView"; import { reportApi } from "../../api/reportApi"; const COLUMNS = [{ key: "drugName", label: "Drug" }, { key: "turnoverRate", label: "Turnover rate" }, { key: "quantity", label: "Quantity on hand" }]; export default function SlowMovingReportPage() { return <ReportView title="Slow Moving" columns={COLUMNS} fetcher={reportApi.getSlowMoving} />; }
