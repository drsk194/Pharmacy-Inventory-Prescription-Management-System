import ReportView from "../../components/reports/ReportView"; import { reportApi } from "../../api/reportApi"; const COLUMNS = [{ key: "date", label: "Date" }, { key: "avgTurnaroundMinutes", label: "Avg turnaround" }]; export default function DispensingTurnaroundReportPage() { return <ReportView title="Dispensing Turnaround" columns={COLUMNS} fetcher={reportApi.getDispensingTurnaround} />; }
