import ReportView from "../../components/reports/ReportView"; import { reportApi } from "../../api/reportApi"; const COLUMNS = [{ key: "drugName", label: "Drug" }, { key: "quantity", label: "Quantity" }, { key: "lastMovementDate", label: "Last movement" }]; export default function DeadStockReportPage() { return <ReportView title="Dead Stock" columns={COLUMNS} fetcher={reportApi.getDeadStock} />; }
