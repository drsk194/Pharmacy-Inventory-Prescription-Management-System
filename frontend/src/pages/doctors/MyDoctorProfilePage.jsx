import { useEffect, useState } from "react";
import { doctorApi } from "../../api/doctorApi";
import StatusBadge from "../../components/common/StatusBadge";
export default function MyDoctorProfilePage() { const [doctor, setDoctor] = useState(null); const [form, setForm] = useState({ phone: "", email: "" }); const [error, setError] = useState(""); const [message, setMessage] = useState(""); useEffect(() => { doctorApi.me().then((response) => { const data = response.data.data; setDoctor(data); setForm({ phone: data.phone || "", email: data.email || "" }); }).catch((err) => setError(err.response?.data?.message || "Couldn't load profile.")); }, []); async function submit(event) { event.preventDefault(); try { await doctorApi.updateMe(form); setMessage("Profile updated."); } catch (err) { setError(err.response?.data?.message || "Couldn't save profile."); } } if (!doctor) return <main className="detail-page">{error || "Loading..."}</main>; return <main className="detail-page"><h1>My profile</h1><div className="badge-row"><StatusBadge label={doctor.verified ? "Verified" : "Pending verification"} tone={doctor.verified ? "success" : "warning"} /><StatusBadge label={doctor.csAuthorized ? "CS Authorized" : "Not CS Authorized"} /></div><form className="modal-form modal-form--standalone" onSubmit={submit}><label>Phone<input value={form.phone} onChange={(event) => setForm({ ...form, phone: event.target.value })} required /></label><label>Email<input type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} required /></label>{error && <p className="form-error">{error}</p>}{message && <p className="form-success">{message}</p>}<button type="submit">Save changes</button></form></main>; }
