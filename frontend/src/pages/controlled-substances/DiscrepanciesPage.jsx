import { useCallback, useEffect, useState } from "react";
import { controlledSubstanceApi } from "../../api/controlledSubstanceApi";
import { isCsAuthorized } from "../../context/csAuthStore";
import CsReauthModal from "../../components/controlled-substances/CsReauthModal";
import DiscrepancyReportModal from "../../components/controlled-substances/DiscrepancyReportModal";
export default function DiscrepanciesPage() { const [rows, setRows] = useState([]); const [error, setError] = useState(""); const [modal, setModal] = useState(null); const load = useCallback(async () => { try { const response = await controlledSubstanceApi.listDiscrepancies(); const data = response.data.data; setRows(data.content || data); } catch (err) { setError(err.response?.data?.message || "Could not load discrepancies."); } }, []); useEffect(() => { const timer = setTimeout(load, 0); return () => clearTimeout(timer); }, [load]); function openReport() { if (isCsAuthorized()) setModal("report"); else setModal("reauth"); } return <main className="list-page"><div className="list-page__header"><h1>Discrepancies</h1><button type="button" onClick={openReport}>Report Discrepancy</button></div>{error && <p className="form-error">{error}</p>}<table className="data-table"><thead><tr><th>Date</th><th>Batch</th><th>Description</th><th>Status</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td>{row.createdAt}</td><td>{row.batchId}</td><td>{row.description}</td><td>{row.status}</td></tr>)}{!rows.length && <tr><td colSpan="4">No discrepancies reported.</td></tr>}</tbody></table>{modal === "reauth" && <CsReauthModal onClose={() => setModal(null)} onSuccess={() => setModal("report")} />}{modal === "report" && <DiscrepancyReportModal onClose={() => setModal(null)} onSaved={() => { setModal(null); load(); }} />}</main>; }
