import { useCallback, useEffect, useState } from "react";
import { drugApi } from "../../api/drugApi";
import Pagination from "../../components/common/Pagination";
import StatusBadge from "../../components/common/StatusBadge";
import DrugFormModal from "../../components/drugs/DrugFormModal";
const TONE = { OTC: "neutral", SCHEDULE_H: "warning", SCHEDULE_H1: "danger", SCHEDULE_X: "danger" };
export default function DrugListPage() { const [rows, setRows] = useState([]); const [page, setPage] = useState(0); const [totalPages, setTotalPages] = useState(0); const [search, setSearch] = useState(""); const [schedule, setSchedule] = useState(""); const [activeOnly, setActiveOnly] = useState(true); const [open, setOpen] = useState(false); const [editing, setEditing] = useState(null); const [error, setError] = useState(""); const load = useCallback(async () => { try { const response = await drugApi.list({ page, size: 20, search: search || undefined, schedule: schedule || undefined, activeOnly: activeOnly || undefined }); const data = response.data.data; setRows(data.content || data); setTotalPages(data.totalPages || 0); } catch (err) { setError(err.response?.data?.message || "Couldn't load drugs."); } }, [page, search, schedule, activeOnly]); useEffect(() => { const timer = setTimeout(load, 300); return () => clearTimeout(timer); }, [load]); async function deactivate(row, event) { event.stopPropagation(); if (!window.confirm(`Deactivate ${row.name}?`)) return; try { await drugApi.setStatus(row.id, false); load(); } catch (err) { setError(err.response?.data?.message || "Couldn't deactivate drug."); } } function saved() { setOpen(false); setEditing(null); load(); } return <main className="list-page"><div className="list-page__header"><h1>Drugs</h1><button type="button" onClick={() => { setEditing(null); setOpen(true); }}>Create Drug</button></div><div className="filter-row"><input className="list-page__search" type="search" placeholder="Search drugs..." value={search} onChange={(event) => { setSearch(event.target.value); setPage(0); }} /><select value={schedule} onChange={(event) => { setSchedule(event.target.value); setPage(0); }}><option value="">All schedules</option>{["OTC", "SCHEDULE_H", "SCHEDULE_H1", "SCHEDULE_X"].map((item) => <option key={item}>{item}</option>)}</select><label className="checkbox-label"><input type="checkbox" checked={activeOnly} onChange={(event) => setActiveOnly(event.target.checked)} />Active only</label></div>{error && <p className="form-error">{error}</p>}<table className="data-table"><thead><tr><th>Name</th><th>Class</th><th>Schedule</th><th>Status</th><th /></tr></thead><tbody>{rows.map((row) => <tr key={row.id} onClick={() => { setEditing(row); setOpen(true); }}><td>{row.name}</td><td>{row.drugClass}</td><td><StatusBadge label={row.schedule} tone={TONE[row.schedule]} /></td><td><StatusBadge label={row.active ? "Active" : "Inactive"} tone={row.active ? "success" : "neutral"} /></td><td>{row.active && <button type="button" onClick={(event) => deactivate(row, event)}>Deactivate</button>}</td></tr>)}{!rows.length && <tr><td colSpan="5">No drugs found.</td></tr>}</tbody></table><Pagination page={page} totalPages={totalPages} onPageChange={setPage} />{open && <DrugFormModal drug={editing} onClose={() => setOpen(false)} onSaved={saved} />}</main>; }
