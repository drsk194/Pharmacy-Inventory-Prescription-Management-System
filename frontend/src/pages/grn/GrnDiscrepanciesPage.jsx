import { useEffect, useState } from "react";
import { grnApi } from "../../api/grnApi";
export default function GrnDiscrepanciesPage() { const [rows, setRows] = useState([]); const [error, setError] = useState(""); useEffect(() => { grnApi.getDiscrepancies().then((response) => { const data = response.data.data; setRows(data.content || data); }).catch((err) => setError(err.response?.data?.message || "Could not load discrepancies.")); }, []); return <main className="list-page"><h1>GRN discrepancies</h1>{error && <p className="form-error">{error}</p>}<table className="data-table"><thead><tr><th>GRN</th><th>Drug</th><th>Ordered</th><th>Received</th><th>Type</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td>#{row.grnId}</td><td>{row.drugName}</td><td>{row.orderedQuantity}</td><td>{row.receivedQuantity}</td><td>{row.discrepancyType}</td></tr>)}{!rows.length && <tr><td colSpan="5">No discrepancies recorded.</td></tr>}</tbody></table></main>; }
