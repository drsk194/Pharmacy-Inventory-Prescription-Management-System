import { useEffect, useState } from "react";
import { grnApi } from "../../api/grnApi";
import BackButton from "../../components/common/BackButton";

export default function GrnDiscrepanciesPage() { const [rows, setRows] = useState([]); const [error, setError] = useState(""); useEffect(() => { grnApi.getDiscrepancies().then((response) => { const data = response.data.data; setRows(data.content || data); }).catch((err) => setError(err.response?.data?.message || "Could not load discrepancies.")); }, []); return <main className="list-page"><BackButton to="/grn" /><h1>GRN discrepancies</h1>{error && <p className="form-error">{error}</p>}<table className="data-table"><thead><tr><th>GRN item</th><th>Drug</th><th>Ordered</th><th>Received</th><th>Type</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td>Item #{row.id} · PO item #{row.purchaseOrderItemId}</td><td>{row.drugGenericName}</td><td>{row.expectedQuantity}</td><td>{row.receivedQuantity}</td><td>{row.quantityDiscrepancy != null && Number(row.quantityDiscrepancy) !== 0 ? "Quantity" : row.qualityDiscrepancy ? "Quality" : "-"}</td></tr>)}{!rows.length && <tr><td colSpan="5">No discrepancies recorded.</td></tr>}</tbody></table></main>; }
