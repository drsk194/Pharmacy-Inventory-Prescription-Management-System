import { useCallback, useEffect, useState } from "react";
import { useParams } from "react-router-dom";
import { grnApi } from "../../api/grnApi";
import SupplierPerformancePanel from "../../components/grn/SupplierPerformancePanel";
export default function GrnDetailPage() { const { id } = useParams(); const [grn, setGrn] = useState(null); const [error, setError] = useState(""); const load = useCallback(async () => { try { const response = await grnApi.getById(id); setGrn(response.data.data); } catch (err) { setError(err.response?.data?.message || "Could not load GRN."); } }, [id]); useEffect(() => { const timer = setTimeout(load, 0); return () => clearTimeout(timer); }, [load]); if (error) return <main className="detail-page"><p className="form-error">{error}</p></main>; if (!grn) return <main className="detail-page">Loading...</main>; return <main className="detail-page"><h1>GRN #{grn.id}</h1><p className="detail-page__meta">PO #{grn.purchaseOrderId} · {grn.supplierName} · {grn.receivedDate || grn.createdAt}</p><h2>Received items</h2><table className="data-table"><thead><tr><th>Drug</th><th>Batch</th><th>Received quantity</th><th>MRP</th><th>Location</th></tr></thead><tbody>{(grn.items || []).map((item) => <tr key={item.id}><td>{item.drugName}</td><td>{item.batchNumber}</td><td>{item.receivedQuantity}</td><td>{item.mrp}</td><td>{item.locationName}</td></tr>)}</tbody></table><h2>Supplier performance</h2><SupplierPerformancePanel supplierId={grn.supplierId} /></main>; }
