import { useEffect, useState } from "react";
import { useLocation, useNavigate } from "react-router-dom";
import { purchaseOrderApi } from "../../api/purchaseOrderApi";
import { supplierApi } from "../../api/supplierApi";
import { drugApi } from "../../api/drugApi";
import PriceComparisonModal from "../../components/purchase-orders/PriceComparisonModal";
export default function PurchaseOrderFormPage() { const location = useLocation(); const navigate = useNavigate(); const [suppliers, setSuppliers] = useState([]); const [drugs, setDrugs] = useState([]); const prefill = location.state?.prefillItem; const [supplierId, setSupplierId] = useState(""); const [items, setItems] = useState([{ drugId: prefill?.drugId || "", quantity: prefill?.quantity || "", unitPrice: "" }]); const [compare, setCompare] = useState(null); const [error, setError] = useState(""); useEffect(() => { Promise.all([supplierApi.list({ page: 0, size: 200, approvedOnly: true, activeOnly: true }), drugApi.list({ page: 0, size: 500, activeOnly: true })]).then(([suppliersResponse, drugsResponse]) => { const supplierData = suppliersResponse.data.data; const drugData = drugsResponse.data.data; setSuppliers(supplierData.content || supplierData); setDrugs(drugData.content || drugData); }).catch(() => setError("Could not load PO options.")); }, []); function update(index, field, value) { setItems(items.map((item, itemIndex) => itemIndex === index ? { ...item, [field]: value } : item)); } async function submit(event) { event.preventDefault(); try { const response = await purchaseOrderApi.create({ supplierId }); const id = response.data.data.id; await purchaseOrderApi.addItems(id, items); navigate(`/purchase-orders/${id}`); } catch (err) { setError(err.response?.data?.message || "Could not create purchase order."); } } return <main className="detail-page"><h1>New purchase order</h1><form className="modal-form modal-form--standalone" onSubmit={submit}><label>Supplier (approved only)<select value={supplierId} onChange={(event) => setSupplierId(event.target.value)} required><option value="">Select...</option>{suppliers.map((supplier) => <option key={supplier.id} value={supplier.id}>{supplier.name}</option>)}</select></label><h2>Items</h2>{items.map((item, index) => <div className="prescription-item-row" key={index}><select value={item.drugId} onChange={(event) => update(index, "drugId", event.target.value)} required><option value="">Select drug...</option>{drugs.map((drug) => <option key={drug.id} value={drug.id}>{drug.name}</option>)}</select><input type="number" min="1" placeholder="Qty" value={item.quantity} onChange={(event) => update(index, "quantity", event.target.value)} required /><input type="number" min="0" step="0.01" placeholder="Unit price" value={item.unitPrice} onChange={(event) => update(index, "unitPrice", event.target.value)} required />{item.drugId && <button type="button" onClick={() => setCompare({ id: item.drugId, name: drugs.find((drug) => String(drug.id) === String(item.drugId))?.name })}>Compare prices</button>}</div>)}<button type="submit">Create purchase order</button>{error && <p className="form-error">{error}</p>}</form>{compare && <PriceComparisonModal drugId={compare.id} drugName={compare.name} onClose={() => setCompare(null)} />}</main>; }
