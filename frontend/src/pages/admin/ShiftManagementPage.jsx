import { useCallback, useEffect, useState } from "react";
import { scheduleApi } from "../../api/scheduleApi";
import { userManagementApi } from "../../api/userManagementApi";
import ShiftFormModal from "../../components/admin/ShiftFormModal";
export default function ShiftManagementPage() { const [rows, setRows] = useState([]); const [users, setUsers] = useState([]); const [open, setOpen] = useState(false); const [error, setError] = useState(""); const load = useCallback(async () => { try { const [shifts, usersResponse] = await Promise.all([scheduleApi.listShifts(), userManagementApi.list({ page: 0, size: 500 })]); const shiftData = shifts.data.data; const userData = usersResponse.data.data; setRows(shiftData.content || shiftData); setUsers(userData.content || userData); } catch (err) { setError(err.response?.data?.message || "Could not load shifts."); } }, []); useEffect(() => { const timer = setTimeout(load, 0); return () => clearTimeout(timer); }, [load]); async function assign(shiftId, userId) { try { await scheduleApi.assignShift(shiftId, userId); load(); } catch (err) { setError(err.response?.data?.message || "Could not assign shift."); } } return <main className="list-page"><div className="list-page__header"><h1>Shift management</h1><button type="button" onClick={() => setOpen(true)}>Create Shift</button></div>{error && <p className="form-error">{error}</p>}<table className="data-table"><thead><tr><th>Name</th><th>Time</th><th>Assigned to</th><th /></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td>{row.name}</td><td>{row.startTime} - {row.endTime}</td><td>{row.assignedUserName || "Unassigned"}</td><td><select defaultValue="" onChange={(event) => event.target.value && assign(row.id, event.target.value)}><option value="">Assign...</option>{users.map((user) => <option key={user.id} value={user.id}>{user.fullName}</option>)}</select></td></tr>)}{!rows.length && <tr><td colSpan="4">No shifts created.</td></tr>}</tbody></table>{open && <ShiftFormModal onClose={() => setOpen(false)} onSaved={() => { setOpen(false); load(); }} />}</main>; }
